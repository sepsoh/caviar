#!/usr/bin/env node


const utils = require('../lib');
const {getCmdType} = require("../lib/utils");
const cmdType = getCmdType();
if(utils[cmdType]){
  utils[cmdType]();
}else{
  console.log('未找到指定命令：',cmdType);
}
