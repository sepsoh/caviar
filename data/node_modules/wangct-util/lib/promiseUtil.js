"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.toPromise = toPromise;
exports.proParse = proParse;

var _typeUtil = require("./typeUtil");

var _arrayUtil = require("./arrayUtil");

/**
 * 转化为promise
 * @param promise
 * @param args
 * @returns {Promise<any>}
 */
function toPromise(promise) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }

  return Promise.resolve((0, _typeUtil.isFunc)(promise) ? promise.apply(void 0, args) : promise);
}
/**
 * promise解析
 * @param target
 * @returns {Promise<unknown[]>|Promise<{}>|Promise<any>}
 */


function proParse(target) {
  if ((0, _typeUtil.isAry)(target)) {
    return Promise.all(target.map(function (item) {
      return toPromise(item);
    }));
  }

  if ((0, _typeUtil.isObj)(target)) {
    var keys = Object.keys(target);
    var proList = keys.map(function (key) {
      return toPromise(target[key]);
    });
    return Promise.all(proList).then(function (result) {
      return (0, _arrayUtil.aryToObject)(keys, function (key) {
        return key;
      }, function (key, index) {
        return result[index];
      });
    });
  }

  return toPromise(target);
}